{
  "name": "peckboard-chicken-coop",
  "version": "0.1.0",
  "private": true,
  "description": "PeckBoard plugin: a 3D chicken run visualizing the cards being worked on — one hen per active card.",
  "scripts": {
    "bundle": "node esbuild.js",
    "build": "node esbuild.js && extism-js dist/index.js -i src/index.d.ts -o dist/plugin.wasm",
    "demo": "esbuild page/main.js --bundle --outfile=.demo/page.js --format=iife --target=es2020",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "^0.166.0"
  },
  "devDependencies": {
    "@extism/js-pdk": "^1.1.1",
    "esbuild": "^0.23.1",
    "vitest": "^2.1.9"
  }
}
